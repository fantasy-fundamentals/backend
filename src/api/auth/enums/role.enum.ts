export enum Role {
  USER = 'user',
  ADMIN = 'admin',
  SUB_ADMIN = 'sub admin',
}

export enum UserTypes {
  CQR_USER = 'standard',
  SDIRA = 'sdira',
  IRA = 'ira',
}

export enum AdminPermissions {
  ALL = '*',
  SUPPORT = 'support',
}
